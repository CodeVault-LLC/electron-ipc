{
  "name": "@codevault/electron-ipc-server",
  "version": "0.0.1",
  "description": "A simple IPC library for Electron applications",
  "main": "dist/server.mjs",
  "types": "dist/server.d.mts",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsup",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@codevault/shared": "workspace:*",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "electron": "^35.1.5"
  },
  "devDependencies": {
    "tsup": "^8.5.0"
  }
}
